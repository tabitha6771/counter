import React from "react";
import { render } from "react-dom";
import App from "./App";

const styles = {
  fontFamily: "Arial",
  textAlign: "center"
};

render(<App />, document.getElementById("root"));
